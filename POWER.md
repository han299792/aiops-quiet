# 검정력 시뮬레이션 결과 (단계 A-8)

2026-09-09. 클러스터·비용 0. 재현: `python -m quiet.analysis.power`

목적: **한 푼 쓰기 전에** "10% 용량을 구분하려면 텔레메트리가 얼마나 필요한가"를
답한다. 만약 10분 창이 저용량에서 부족하다면 곡선 하단은 신호가 아니라 잡음이고,
에이전트 비용을 아무리 써도 고쳐지지 않는다.

## 기본 가정에서의 결과

`baseline_error=0.2%, charge_fraction=25%, services=12, α=0.05`

```
spans/svc |    0% |   10% |   25% |   50% |  100%
-------------------------------------------------
      500 |    0% |   55% |  100% |  100% |  100%
     1000 |    0% |   98% |  100% |  100% |  100%
     2000 |    0% |  100% |  100% |  100% |  100%
```

0% 열이 거짓양성률이고 0~1%로 α/9 근처에 정확히 떨어진다 — 보정이 제대로
작동한다는 뜻이다.

## ★ 결론 1: 곡선의 모양이 아직 모르는 두 숫자에 달려 있다

`spans/svc=1000`, `dose=10%`를 고정하고 흔들어 본 결과:

| charge_fraction | 탐지율 |     | baseline_error | 탐지율 |
|---|---|---|---|---|
| 50% | 100% | | 0.05% | 99% |
| 25% | 98%  | | 0.20% | 99% |
| **10%** | **20%** | | **1.00%** | **72%** |
| 5%  | 2%   | | 5.00% | 16% |
| 2%  | 0%   | | 15.0% | 5% |

- **`charge_fraction`의 무릎이 10~25% 사이에 있다.** `paymentFailure`는 결제
  요청만 실패시키는데, 결제가 payment 서비스 span의 몇 %인지 모른다. 25%면
  10% 용량도 이미 명시적이고, 5%면 100% 용량조차 조용하다.
- **기저 에러율이 5%를 넘으면 10% 용량이 묻힌다.** astronomy-shop이 평상시
  얼마나 에러를 내는지 모른다.

→ **단계 B에서 반드시 측정할 것 세 개**: 서비스별 분당 span 수, payment 서비스
span 중 charge 비율, 정상 상태 에러율. 이 셋을 넣고 이 시뮬레이션을 다시 돌린
뒤에 창 길이와 사다리를 확정한다. **측정 전에 사다리를 고정하지 않는다.**

## ★ 결론 2: 창 길이가 용량과 독립인 두 번째 축이다

명시성은 "장애 크기"만의 함수가 아니라 **"장애 크기 × 관측량"** 의 함수다.
같은 10% 용량이 span 500개에선 55%, 2000개에선 100% 탐지된다.

그래서 실험은 1차원 사다리가 아니라 **2차원 격자**로 볼 수 있다:

|  | 짧은 창 | 긴 창 |
|---|---|---|
| **저용량** | 가장 조용함 | 중간 |
| **고용량** | 중간 | 가장 시끄러움 |

그리고 결정적으로 — **원본(RawWindow)을 보관하므로 긴 창 하나를 뜬 뒤
오프라인에서 잘라 짧은 창들을 만들 수 있다.** 추가 클러스터 시간 0으로
명시성 축 전체를 훑는다. 3단계 설계(Raw → Snapshot → Verdict)가 여기서
값을 한다.

**전제**: RawWindow는 span별 타임스탬프를 그대로 보관해야 한다. 히스토그램으로
집계해버리면 잘라낼 수 없다. → 수집기 요구사항으로 확정.

## ★ 결론 3: 포화되면 그게 더 흥미로운 결과다

`charge_fraction`이 25%로 나오면 10% 용량도 텔레메트리상 이미 100% 명시적이고,
명시성 곡선은 상단에서 평평해진다. H1이 시시해지는 것처럼 보이지만 아니다 —
그러면 질문이 이렇게 바뀐다:

> **신호가 명백히 거기 있었는데도 에이전트가 못 맞혔는가?**

이건 F1(접근조차 안 함) / F2(읽고도 못 알아봄) / F7(데이터에 답이 없음)의
3분할이 정확히 답하는 질문이고, 원래 연구 질문(§PREREG 1)에 더 직접적이다.
포화는 실패가 아니라 F7을 배제해주는 조건이다.

반대로 조용한 쪽으로 나오면 H1이 살고 곡선이 흥미로워진다. 어느 쪽이든 결과가 있다.

## 이 결과가 PREREG에 미치는 영향

§5(창 길이와 반복 수는 시뮬레이션이 정한다)의 잠정값 "창 10분"을 아직 확정하지
않는다. 단계 B의 측정 후 확정하고 §11 개정 이력에 기록한다.
