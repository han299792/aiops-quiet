# 사전등록 (pre-registration)

**최초 커밋: 2026-09-09.** 이 파일은 **데이터를 보기 전에** 커밋된다.
이후 수정은 금지가 아니라 **추적 가능**해야 한다 — 바꿀 일이 생기면 아래
§9 개정 이력에 무엇을·왜 바꿨는지 적고, 변경 시점이 어떤 데이터를 본 뒤인지
명시한다. 조용한 수정은 사후 합리화와 구분되지 않는다.

값이 아니라 **절차**를 못박는 것이 목적이다. 임계값·효과 크기처럼
데이터가 정해야 하는 숫자는 여기서 정하지 않는다. 대신 그것을 **어떻게**
정할지를 정한다.

---

## 1. 연구 질문

에이전트가 장애를 탐지하지 못했을 때, 그 원인은 셋 중 무엇인가.

- **(a) 데이터에 답이 없다** — 텔레메트리에 신호 자체가 없다
- **(b) 축약이 답을 지웠다** — 원본에는 있으나 에이전트가 받는 뷰에는 없다
- **(c) 보고도 못 알아봤다** — 신호가 컨텍스트에 들어왔는데 결론이 틀렸다

## 2. 주 가설

**H1 (주 가설).** 텔레메트리에 장애가 얼마나 명시적으로 적혀 있는가(명시성)는
주입된 장애의 크기(용량)에 따라 **단조 증가**한다.

**H2.** 에이전트의 탐지율도 용량에 따라 **단조 증가**한다.

**H3.** 명시성이 낮은 구간(저용량)에서 탐지율의 **회차 간 분산이 더 크다.**

**H0로 취급할 것**: 위 셋 중 어느 것도 성립하지 않을 수 있다. 곡선이 평평하게
나오면 그것을 결과로 보고한다. 사후에 조건을 바꿔 곡선을 만들지 않는다.

## 3. 조건

앱은 **astronomy-shop** 고정. 장애는 `paymentFailure` 피처 플래그.

| 조건 ID | variant | 정답 | 역할 |
|---|---|---|---|
| `payment_dose_00-detection-1` | `off` | No | **sham 대조군** |
| `payment_dose_10-detection-1` | `10%` | Yes | |
| `payment_dose_25-detection-1` | `25%` | Yes | |
| `payment_dose_50-detection-1` | `50%` | Yes | |
| `payment_dose_100-detection-1` | `100%` | Yes | |
| `noop_detection_astronomy_shop-1` | — (주입 없음) | No | 보조 대조군 |
| `pod_kill_hotel_res-detection-1` | Chaos Mesh | Yes | 시끄러움 기준선 |

**sham 대조군의 정의가 이 실험의 핵심이다.** 0% 조건은 "아무것도 안 하는 것"이
아니라 `variant="off"`로 **configmap 쓰기와 `kubectl rollout restart deployment
flagd`를 다른 용량과 완전히 동일하게 수행**한다. 행동적 효과만 0이다.

이유: `OtelFaultInjector`가 주입 끝에 flagd를 재시작하므로, 주입 행위 자체가
파드 교체 이벤트를 남긴다. 주입을 하지 않는 `noop`을 0% 대조군으로 쓰면
이벤트 채널이 10~100%에서 전부 1, 0%에서만 0이 되어 **장애가 아니라 주입 도구
때문에** 완벽한 용량-반응 계단이 만들어진다. sham은 그 인공물을 상쇄한다.

`noop`은 sham과 비교해 "주입 행위가 남긴 흔적의 크기"를 재기 위해 남긴다.

## 4. 표본 수와 그것으로 말할 수 있는 것

- E-1(프로브, 에이전트 없음): 조건당 R회. R은 §5의 검정력 시뮬레이션이 정한다.
- E1(에이전트): 조건당 **N=10**.

**N=10으로 말할 수 있는 것과 없는 것을 미리 적어둔다.**
관측 성공률 5/10의 Wilson 95% CI는 약 [0.24, 0.76]으로 폭이 0.5다.
두 조건 차이가 유의하려면 대략 0.5 이상 벌어져야 한다.

→ **N=10은 "이 현상이 존재한다"를 보이는 탐색용이며, 조건 간 미세 비교용이
아니다.** 결론을 그렇게만 쓴다. 점추정을 단독으로 보고하지 않고 항상 CI를
병기한다. 곡선의 **단조성**은 개별 점 비교보다 강한 증거이므로 그쪽을 주로 본다.

## 5. 창 길이와 반복 수는 시뮬레이션이 정한다

T_warm / 창 길이 / R을 지금 임의로 정하지 않는다. 대신:

1. 알려진 효과 크기와 알려진 span·로그 물량으로 합성 스냅샷 쌍을 생성한다
2. 보정 → 채점 파이프라인을 통째로 돌린다
3. **가장 낮은 용량(10%)을 구분하는 데 필요한 창 길이와 R**을 읽는다

이 시뮬레이션은 클러스터·비용 없이 노트북에서 돌고, **첫 클러스터 실행 전에**
끝낸다. 결과는 `POWER.md`에 있다.

**시뮬레이션이 실제로 답한 것 (2026-09-09):** 창 길이를 지금 확정할 수 없다.
곡선의 모양이 아직 측정하지 않은 두 숫자에 달려 있기 때문이다.

- `charge_fraction`(payment 서비스 span 중 결제 요청의 비율)의 무릎이 10~25%
  사이에 있다. 25%면 10% 용량도 이미 명시적이고, 5%면 100% 용량조차 조용하다.
- 기저 에러율이 5%를 넘으면 10% 용량이 묻힌다.

→ **단계 B에서 세 값을 측정한 뒤에 창 길이와 사다리를 확정하고 §11에 기록한다.**
측정 전에 고정하지 않는다. 잠정값: T_warm 5분, 창 10분, T_settle 90초.

**추가로 확정된 설계 요구사항**: 명시성은 "장애 크기 × 관측량"의 함수이므로
창 길이가 용량과 독립인 두 번째 축이다. 긴 창 하나를 뜬 뒤 **오프라인에서 잘라**
짧은 창들을 만들면 추가 클러스터 비용 없이 이 축을 훑을 수 있다. 이를 위해
**RawWindow는 span별 타임스탬프를 그대로 보관해야 한다** — 히스토그램으로
집계해 버리면 잘라낼 수 없다.

**포화가 나와도 결과다.** 10% 용량이 이미 명시성 3이면 H1은 평평해지지만,
질문이 "신호가 명백히 있었는데도 에이전트가 못 맞혔는가"로 바뀌고 이건 §1의
3분할(F1/F2/F7)이 더 직접적으로 답한다. 포화는 F7을 배제해 주는 조건이다.

## 6. 명시성 측정 — 절차

정상 창과 장애 창을 뜨고 3채널 각각에 0/1 판정. 점수 0~3.

| 채널 | 하위 통계량 |
|---|---|
| C-이벤트 | 경고 이벤트 율비 z, 컨테이너 restart 델타(불리언 승격) |
| C-로그 | ERROR급 줄 비율의 2표본 비율 z |
| C-메트릭 | 에러 span 비율 z, 지연 Brunner–Munzel, 자원 Welch t |

채널 내부는 하위 통계량의 최댓값. 채널 간 k=3에만 Bonferroni.

### 임계값 결정 절차 (값이 아니라 절차를 고정한다)

1. sham(0%)을 R회 돌려 **연속한 정상 창 두 개**를 얻는다. 둘 다 실제로 정상이다.
2. `(W1,W2)`와 `(W2,W1)` **양방향 모두**를 귀무쌍으로 넣는다.
3. `임계 = 귀무분포의 (1 − α/k) 분위수`, **α = 0.05, k = 3.**
4. 귀무 표본이 20 미만이면 분위수가 사실상 최댓값이 되므로
   `중앙값 + 3 × 1.4826 × MAD`로 대체하고 **어느 방법을 썼는지 기록한다.**
5. **홀드아웃**: 절반으로 임계를 잡고 나머지 절반에서 실제 거짓양성률을 잰다.

**임계를 정한 뒤에는 바꾸지 않는다.** 바꿔야 할 이유가 생기면 전 조건을
**전량 재계산**하고 §9에 기록한다. 일부 조건만 다시 채점하는 것은 금지한다.

### 측정기 검증 (이걸 통과하지 못하면 결과를 보고하지 않는다)

- (a) sham(0%)에서 명시성 **0**
- (b) `pod_kill`에서 명시성 **3 이상**
- (c) 홀드아웃 거짓양성률 ≈ α

## 7. 주 결과 지표

**1차 지표**: 용량에 대한 **명시성 마진**(= 통계량 − 임계, 연속값)의 단조성.
정수 점수 0~3은 고용량에서 포화되므로 헤드라인 표시용으로만 쓰고 회귀에는
마진을 쓴다.

**2차 지표**: 용량에 대한 에이전트 탐지율(Wilson CI 병기).

**3차 지표**: 명시성 ↔ 탐지율 산점도.

## 8. 분석 규칙 (데이터를 보기 전에 고정)

### 8.1 폐기와 기록
리셋 검증에 실패한 회차는 **버리고, 버렸다는 사실을 `discarded.jsonl`에
기록한다.** 폐기율 자체가 보고할 숫자다. 조용히 재시도하지 않는다.
폐기된 회차는 N에 산입하지 않는다.

### 8.2 정답 누출 처리 — 버리지 않고 검열한다
에이전트는 임의 셸(`exec_shell`)을 갖고 있어 `kubectl get cm flagd-config -o yaml`로
답을 직접 읽을 수 있다.

- **ACTION 계열**(에이전트가 시도한 것)과 **OBSERVATION 계열**(실제로 컨텍스트에
  들어온 것)을 분리한다. 결정적인 것은 OBSERVATION뿐이다.
- `first_leak_step`을 기록한다. 제출이 누출보다 **앞이면 깨끗한 회차로 쓴다.**
  뒤면 `leak_censored`로 표시한다.
- **곡선을 두 번 그린다 — 전체, 그리고 깨끗한 것만.** 두 곡선이 갈리면
  그 격차 자체가 보고할 결과다.

누출 회차를 통째로 버리지 않는 이유: 그러면 "텔레메트리로 추론하는 에이전트"만
표본에 남아, 정확히 재려던 모집단이 깨진다.

### 8.3 거짓양성 보정
sham·noop 조건에서의 "장애 있음" 응답률이 거짓양성률이다. 이것이 높으면
에이전트가 습관적으로 "있음"이라 답하는 것이므로, 저용량 탐지율을 원값으로
쓰지 않고 **거짓양성 보정 후** 보고한다.

### 8.4 독립성 점검
회차 순서 대비 성공률에 추세가 있는지 그린다. 추세가 보이면 회차 간 독립
가정이 깨진 것이므로 그 사실을 보고한다.

### 8.5 실패 지점 코딩
F1 접근조차 안 함 / F2 읽고도 못 알아봄 / F3 스텝 소진 / F4 형식 실패 /
F5 조기 제출 / F6 환경 오류 / F7 데이터에 답이 없음.

- 코딩 시 **조건 라벨을 가린다**(파일명에서 조건 제거, 셔플).
- 전체의 20%를 **하루 간격으로 재코딩**해 자기일치율을 낸다.
- 80% 미만이면 코드 정의가 모호한 것이므로 정의를 고치고 **전량 재코딩**한다.

F1↔F2↔F7의 비율이 §1의 3분할에 대한 답이다.

### 8.6 무판정 기준
회차 간 분산이 조건 간 차이보다 크면 **"차이를 말할 수 없다"** 고 쓴다.
구체적으로: 차이의 95% CI가 0을 포함하면 무판정으로 보고한다.

## 9. 실험 조건 고정값

- 모델: 하나로 고정 (§9 개정 이력에 확정 시점 기록)
- **temperature = 0** (업스트림 하드코딩 0.5를 쓰지 않는다). 반복의 분산이
  표집잡음이 아니라 환경잡음에서만 오게 하기 위함
- **LLM 응답 캐시 OFF.** 캐시 키에 모델·샘플링 파라미터가 없고, 조건 간 초반
  턴이 거의 동일해 한 조건의 추론이 다른 조건에 배달될 수 있다
- max_steps: 파일럿에서 확정 후 여기 기록
- 앱 helm 차트 버전: 핀 후 여기 기록
- 예산 상한: LLM **$40**. 초과 시 즉시 중단하고 거기까지를 결과로 보고한다

## 10. 중단 규칙

- 예산 $40 초과 → 즉시 중단, 부분 결과 보고
- 셋업 누적 6시간 초과 → 폴백(스냅샷 방식)으로 전환
- E1 벽시계 3일 초과 → **N을 줄이지 말고 조건을 줄인다**
- §6 측정기 검증 (a)(b)(c) 중 하나라도 실패 → 에이전트를 붙이지 않고 측정기부터 고친다

## 11. 개정 이력

| 날짜 | 무엇을 | 왜 | 이 시점에 본 데이터 |
|---|---|---|---|
| 2026-09-09 | 최초 작성 | — | 없음 (레포 코드만 읽음) |
| 2026-09-09 | §5 갱신: 창 길이 확정을 단계 B 이후로 미룸. RawWindow가 span 타임스탬프를 보관해야 한다는 요구사항 추가 | 검정력 시뮬레이션(`POWER.md`) 결과, 곡선의 모양이 미측정 파라미터 2개(charge_fraction, 기저 에러율)에 좌우됨이 드러남 | **합성 데이터만.** 클러스터 데이터 없음 |
